# Deprecated: Unified API Key Profiles

This document referenced legacy third-party provider templates that are out of scope for this fork.

Current fork scope:
- Keep core providers in active flows: Claude, Codex, Gemini.
- Remove legacy provider references from setup/UI/docs.

If new providers are reintroduced later, document them only after official support lands in this fork.
